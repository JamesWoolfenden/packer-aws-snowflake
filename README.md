# Packer-aws-snowflake

Packer template to create Snowflake server for Amazon Linux 2

This template uses the Packer HCL2, using Packer 1.5.5 or later. 

## Usage:

```bash
 packer build snowflake
```